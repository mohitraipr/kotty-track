<%- include('partials/header', { pageTitle: 'Challan List' }) %>
<%- include('partials/navbar', { user: user, dashboardUrl: '/challandashboard' }) %>

<!-- Main Content -->
<main class="main-content" style="margin-left: 0;">
  <div class="container-fluid" style="max-width: 1400px; margin: 0 auto;">

    <!-- Page Header -->
    <div class="page-header">
      <div class="breadcrumbs">
        <a href="/challandashboard">Dashboard</a>
        <span class="separator">/</span>
        <span>Challan List</span>
      </div>
      <h1 class="page-title">Challan List</h1>
      <p class="page-subtitle">View and manage all challans</p>
    </div>

    <!-- Alerts -->
    <% if (error && error.length) { %>
      <div class="alert alert-danger">
        <i class="bi bi-exclamation-circle"></i>
        <div>
          <% error.forEach(msg => { %><p class="mb-0"><%= msg %></p><% }); %>
        </div>
      </div>
    <% } %>
    <% if (success && success.length) { %>
      <div class="alert alert-success">
        <i class="bi bi-check-circle"></i>
        <div>
          <% success.forEach(msg => { %><p class="mb-0"><%= msg %></p><% }); %>
        </div>
      </div>
    <% } %>

    <!-- Search Card -->
    <div class="card mb-4">
      <div class="card-body">
        <form method="GET" action="/challanlist" class="d-flex flex-wrap gap-3 align-items-end">
          <div class="flex-grow-1" style="min-width: 250px; max-width: 400px;">
            <label class="form-label">Search Challans</label>
            <div class="position-relative">
              <input
                type="text"
                name="search"
                value="<%= search %>"
                class="form-input"
                placeholder="Search by Challan No or Lot No"
                style="padding-left: 40px;"
              >
              <i class="bi bi-search position-absolute" style="left: 14px; top: 50%; transform: translateY(-50%); color: var(--text-muted);"></i>
            </div>
          </div>
          <button type="submit" class="btn btn-primary">
            <i class="bi bi-search"></i> Search
          </button>
          <% if (search) { %>
            <a href="/challanlist" class="btn btn-secondary">
              <i class="bi bi-x-lg"></i> Clear
            </a>
          <% } %>
        </form>
      </div>
    </div>

    <!-- Challans Table -->
    <div class="card">
      <div class="card-header">
        <h2 class="card-title">
          <i class="bi bi-file-earmark-text me-2" style="color: var(--terracotta);"></i>
          All Challans
        </h2>
        <% if (challans && challans.length) { %>
          <span class="badge badge-primary"><%= challans.length %> records</span>
        <% } %>
      </div>

      <div class="table-container" style="border: none; border-radius: 0;">
        <div class="table-responsive">
          <table class="table">
            <thead>
              <tr>
                <th>ID</th>
                <th>Challan No</th>
                <th>Date</th>
                <th>Consignee</th>
                <th>Total Value</th>
                <th style="text-align: center;">Actions</th>
              </tr>
            </thead>
            <tbody>
              <% if (challans && challans.length) { %>
                <% challans.forEach(ch => { %>
                  <tr>
                    <td>
                      <span class="badge badge-neutral">#<%= ch.id %></span>
                    </td>
                    <td>
                      <span style="font-weight: 500; color: var(--text-primary);"><%= ch.challan_no %></span>
                    </td>
                    <td>
                      <% if (ch.challan_date) { %>
                        <div class="d-flex align-items-center gap-2">
                          <i class="bi bi-calendar3" style="color: var(--text-muted); font-size: 0.875rem;"></i>
                          <%= ch.challan_date.toLocaleDateString('en-CA') %>
                        </div>
                      <% } else { %>
                        <span class="text-muted">--</span>
                      <% } %>
                    </td>
                    <td><%= ch.consignee_name %></td>
                    <td>
                      <span style="font-weight: 500; color: var(--terracotta);">
                        <%= ch.total_amount ? '₹' + Number(ch.total_amount).toLocaleString('en-IN') : '--' %>
                      </span>
                    </td>
                    <td style="text-align: center;">
                      <a href="/challandashboard/view/<%= ch.id %>" class="btn btn-sm btn-primary">
                        <i class="bi bi-eye"></i> View/Print
                      </a>
                    </td>
                  </tr>
                <% }); %>
              <% } else { %>
                <tr>
                  <td colspan="6">
                    <div class="empty-state">
                      <div class="empty-state-icon">
                        <i class="bi bi-file-earmark-x" style="font-size: 3rem;"></i>
                      </div>
                      <h3 class="empty-state-title">No Challans Found</h3>
                      <p class="empty-state-text">
                        <% if (search) { %>
                          No challans match your search criteria. Try adjusting your search terms.
                        <% } else { %>
                          There are no challans to display at the moment.
                        <% } %>
                      </p>
                      <% if (search) { %>
                        <a href="/challanlist" class="btn btn-secondary mt-3">
                          <i class="bi bi-arrow-left"></i> Back to All Challans
                        </a>
                      <% } %>
                    </div>
                  </td>
                </tr>
              <% } %>
            </tbody>
          </table>
        </div>
      </div>
    </div>

  </div>
</main>

<style>
  /* Page-specific styles */
  .main-content {
    margin-left: 0 !important;
  }

  .table-responsive {
    overflow-x: auto;
    -webkit-overflow-scrolling: touch;
  }

  @media (max-width: 768px) {
    .page-header {
      margin-bottom: 20px;
    }

    .card-header {
      flex-direction: column;
      align-items: flex-start !important;
      gap: 8px;
    }

    .table th,
    .table td {
      padding: 10px 12px;
      white-space: nowrap;
    }

    .btn-sm {
      padding: 6px 10px;
      font-size: 0.75rem;
    }
  }
</style>

<%- include('partials/footer') %>
